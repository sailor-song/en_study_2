# 单词对对碰游戏详细设计

## 1. 游戏概述

**游戏名称**: 单词对对碰
**入口**: 主导航"测试"后新增"对碰"选项
**目标**: 在倒计时内，将打乱的单词与释义正确匹配

## 2. 难度设计

| 难度 | 每组单词数 | 每组时间 | 总组数 |
|------|-----------|---------|-------|
| 简单 | 5 | 30秒 | 3组 (15词) |
| 中等 | 5 | 25秒 | 4组 (20词) |
| 困难 | 5 | 20秒 | 5组 (25词) |

## 3. 游戏流程

### 3.1 开始界面
- 显示游戏标题和图标
- 难度选择器 (简单/中等/困难)
- 显示当前单词本总词数
- "开始游戏" 按钮

### 3.2 游戏进行中
1. 从单词本随机抽取单词 (根据难度决定数量)
2. 将单词分为5个一组
3. 每组单词拆分为:
   - 左侧: 单词列表 (随机顺序)
   - 右侧: 释义列表 (随机顺序)
4. 玩家操作:
   - 点击左侧单词按钮 (选中态)
   - 点击右侧释义按钮 (匹配)
   - 匹配正确: 两按钮变绿并消失，显示连线
   - 匹配错误: 两按钮闪红后恢复
5. 一组5个全部匹配后:
   - 显示短暂的成功动画
   - 切换到下一组
6. 倒计时归零时:
   - 游戏结束
   - 显示完成的组数和正确率

### 3.3 结束界面
- 显示结果 (成功/失败)
- 正确匹配数 / 总匹配数
- 完成的组数
- "再玩一次" 按钮
- "返回主页" 按钮

## 4. 界面布局

```
┌─────────────────────────────────────────────────┐
│  [计时器]     第 1/4 组          [当前得分]      │
├─────────────────────────────────────────────────┤
│                                                 │
│   ┌─────────┐              ┌─────────┐         │
│   │  word1  │              │   def1  │         │
│   └─────────┘              └─────────┘         │
│   ┌─────────┐              ┌─────────┐         │
│   │  word2  │      ───     │   def2  │         │
│   └─────────┘      ╱╲      └─────────┘         │
│   ┌─────────┐     ╱  ╲     ┌─────────┐         │
│   │  word3  │    ╱    ╲    │   def3  │         │
│   └─────────┘   ──    ──   └─────────┘         │
│   ┌─────────┐    ╲    ╱    ┌─────────┐         │
│   │  word4  │     ╲  ╱     │   def4  │         │
│   └─────────┘      ╲╱      └─────────┘         │
│   ┌─────────┐      ───     ┌─────────┐         │
│   │  word5  │              │   def5  │         │
│   └─────────┘              └─────────┘         │
│                                                 │
│              [已匹配: 3/5]                       │
└─────────────────────────────────────────────────┘
```

## 5. 交互状态

### 按钮状态
- **默认**: 紫色边框，白色文字
- **选中 (单词侧)**: 蓝色背景高亮
- **匹配成功**: 绿色背景，逐渐消失
- **匹配失败**: 红色闪烁动画

### 匹配动画
- 正确匹配时，两按钮间显示绿色连线
- 连线在0.5秒后消失，按钮淡出

## 6. 技术实现

### 数据结构
```javascript
{
  difficulty: 'easy' | 'medium' | 'hard',
  allWords: [{word, definition, id}], // 本局所有单词
  groups: [[word, word, word, word, word], ...], // 分组
  currentGroupIndex: 0,
  currentGroup: {words: [], definitions: [], matched: []},
  score: 0,
  totalMatched: 0,
  timeLeft: 30,
  gameState: 'setup' | 'playing' | 'complete' | 'timeout'
}
```

### 关键函数
- `startMatchGame()` - 初始化游戏
- `prepareGroup(index)` - 准备当前组
- `selectWord(word)` - 选中单词
- `selectDefinition(definition)` - 选择释义
- `checkMatch(word, definition)` - 验证匹配
- `onGroupComplete()` - 组完成处理
- `onGameComplete()` - 游戏结束
- `updateTimer()` - 更新倒计时

## 7. 样式规范

- 与现有应用风格一致
- 使用现有颜色变量
- Material Symbols 图标
- 圆角卡片设计
- 流畅的过渡动画
